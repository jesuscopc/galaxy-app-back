import * as topsecret_split from '../../routes/topsecret_split';

describe('Suit test topsecret_split Page', () => {
  test('should be defined', () => {
    expect(topsecret_split).toBeDefined();
  });
})
